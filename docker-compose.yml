services:
  wud:
    image: fmartinou/whats-up-docker
    container_name: wud
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=${TZ}
      - WUD_TRIGGER_HTTP_NTFY_URL=${NTFY_URL}/${NTFY_TOPIC}?auth=${NTFY_TOKEN}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - frontend
      - wud-net

networks:
  frontend:
    external: true
  wud-net:
    name: wud-net
    driver: bridge
